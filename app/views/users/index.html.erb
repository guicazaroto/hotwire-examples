<div class="container">
  <h1>Usuários</h1>
  
  <%= form_with url: users_path,
    method: :get,
    data: {
      turbo_frame: 'search',
      controller: 'search',
      action: 'keyup->search#perform'
    } do |f|  %>
    <%= f.label :query, "Search for:", class: "form-label" %>
    <%= f.text_field :query, class: "form-control", value: params[:query] %>
  <% end %>
  
  <%= render "users_table", users: @users %>
</div>
